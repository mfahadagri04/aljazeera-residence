services:
  - type: web
    name: aljazeera-server
    runtime: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: MONGODB_URI
        value: your-mongodb-atlas-uri
      - key: PORT
        value: 10000